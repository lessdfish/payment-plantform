# 渠道模拟器 — 本地开发配置

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/payment_simulator?useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: root123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
