# 账户服务 — 本地开发配置
# 生产配置通过 Nacos 下发，此文件仅用于本地开发

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/payment_account?useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: root123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
  cloud:
    sentinel:
      transport:
        dashboard: localhost:8858
